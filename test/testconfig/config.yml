host: cluster.example.com
user: the_user
private_keyfile: /home/user/.ssh/keyfile

copy:
  - from: myfile.txt
    to: mycopy.txt

  - from: slurm.job
    to: slurm.job

collect:
  - result.txt

clean:
  - mycopy.txt
  - slurm.job

sbatch: slurm.job